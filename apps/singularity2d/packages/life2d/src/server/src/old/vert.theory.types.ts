type ID_TAG = string;
type KEYWORD_STRING = string;
type PRIVATE_KEY = Uint8Array | string;
type PUBLIC_KEY = Uint8Array | string;
type BASE_64_MIME_IMAGE = string;

type SIGNATURE = string;

type TITLE_STRING = string;
type SUMMARY_STRING = string;
type DESCRIPTION_STRING = string;

type VERT_TOKEN = number;